<header class="header">
  <div class="container navbar">
    <a href="/" class="logo" aria-label="Retour à l’accueil RAA Clean Services">
      <img src="/logo.jpeg" alt="RAA Clean Services logo" />
    </a>

      <form class="search-form" action="/search" method="get" role="search" aria-label="Recherche">
      <label class="sr-only" for="search-input">Rechercher</label>
      <input id="search-input" name="q" type="search" placeholder="Rechercher..." />
      <button type="submit">OK</button>
    </form>

    <nav class="nav" aria-label="Navigation principale">
      <a href="/#home">Accueil</a>
      <a href="/#services">Services</a>
      <a href="/#gallery">Galerie</a>
      <a href="/contact" class="nav-link">Contact</a>
    </nav>
  </div>
</header>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    width: 100%;
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(16px);
    z-index: 1000;
    box-shadow: 0 16px 44px rgba(15, 23, 42, 0.08);
  }

  .navbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 18px 0;
  }

  .logo {
    display: flex;
    align-items: center;
    text-decoration: none;
    flex: 0 0 auto;
  }

  .search-form {
    flex: 1 1 420px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }

  .logo img {
    height: 64px;
    max-width: 220px;
    width: auto;
    object-fit: contain;
    transition: transform 0.25s ease, filter 0.25s ease;
  }

  .logo img:hover {
    transform: translateY(-2px);
    filter: drop-shadow(0 10px 16px rgba(47, 167, 74, 0.18));
  }

  .nav {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  .search-form input {
    width: 100%;
    max-width: 340px;
    padding: 0.65rem 0.9rem;
    border: 1px solid rgba(31, 41, 55, 0.12);
    border-radius: 999px;
    background: #f8fbff;
    color: #1f2937;
    outline: none;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }

  .search-form input:focus {
    border-color: rgba(47, 167, 74, 0.45);
    box-shadow: 0 0 0 4px rgba(47, 167, 74, 0.12);
  }

  .search-form button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.65rem 1rem;
    border-radius: 999px;
    border: none;
    background: var(--secondary);
    color: #ffffff;
    font-weight: 600;
    cursor: pointer;
    transition: opacity 0.2s ease, transform 0.2s ease;
  }

  .search-form button:hover {
    opacity: 0.9;
    transform: translateY(-1px);
  }

  .nav a {
    text-decoration: none;
    color: #1f2937;
    font-weight: 600;
    position: relative;
    padding: 0.25rem 0;
  }

  .nav a::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: -6px;
    width: 0;
    height: 2px;
    background: var(--secondary);
    transition: width 0.3s ease;
  }

  .nav a:hover::after {
    width: 100%;
  }

  .nav-link {
    color: #1f2937;
  }

  @media (max-width: 960px) {
    .navbar {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
      padding: 10px 0;
      gap: 0.5rem;
    }

    .logo img {
      height: 40px;
    }

    .nav {
      justify-content: center;
      flex-wrap: wrap;
      gap: 0.7rem;
      order: 3;
      flex: 1;
    }

    .search-form {
      display: none;
    }

    .search-form input {
      width: 100%;
      min-width: 0;
      max-width: none;
      padding: 0.55rem 0.75rem;
      font-size: 0.9rem;
    }

    .search-form button {
      padding: 0.55rem 0.8rem;
      font-size: 0.85rem;
    }
  }

  @media (max-width: 768px) {
    .navbar {
      padding: 8px 0;
      gap: 0.3rem;
    }

    .logo img {
      height: 36px;
    }

    .nav {
      gap: 0.6rem;
      font-size: 0.8rem;
    }

    .nav a {
      font-size: 0.8rem;
      padding: 0.1rem 0;
    }

    .search-form {
      display: none;
    }

    .search-form input {
      padding: 0.5rem 0.7rem;
      font-size: 0.88rem;
      border-radius: 6px;
    }

    .search-form button {
      padding: 0.5rem 0.75rem;
      font-size: 0.8rem;
    }
  }

  @media (max-width: 640px) {
    .header {
      box-shadow: 0 4px 16px rgba(15, 23, 42, 0.04);
    }

    .navbar {
      gap: 0.3rem;
      padding: 6px 0;
      flex-direction: row;
    }

    .logo {
      flex-shrink: 0;
    }

    .logo img {
      height: 32px;
      max-width: 150px;
    }

    .nav {
      gap: 0.5rem;
      font-size: 0.75rem;
      flex: 1;
    }

    .nav a {
      font-size: 0.75rem;
      padding: 0.1rem 0;
      white-space: nowrap;
    }

    .nav a::after {
      bottom: -3px;
      height: 1px;
    }

    .search-form {
      display: none;
    }

    .search-form input {
      padding: 0.45rem 0.65rem;
      font-size: 0.8rem;
      border-radius: 6px;
    }

    .search-form button {
      width: 100%;
      padding: 0.45rem 0.6rem;
      font-size: 0.75rem;
    }
  }

  @media (max-width: 480px) {
    .navbar {
      gap: 0.25rem;
      padding: 4px 0;
    }

    .logo img {
      height: 28px;
      max-width: 140px;
    }

    .nav {
      gap: 0.4rem;
      font-size: 0.7rem;
      flex: 1;
    }

    .nav a {
      font-size: 0.7rem;
      padding: 0.08rem 0;
      white-space: nowrap;
    }

    .search-form {
      display: none;
    }
  }
</style>
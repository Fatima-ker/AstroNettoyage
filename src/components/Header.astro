<header class="header">
  <div class="container navbar">
    <a href="/" class="logo" aria-label="Retour à l’accueil CleanPro">
      <img src="/logo.svg" alt="CleanPro logo" />
    </a>

      <form class="search-form" action="/search" method="get" role="search" aria-label="Recherche">
      <label class="sr-only" for="search-input">Rechercher</label>
      <input id="search-input" name="q" type="search" placeholder="Rechercher..." />
      <button type="submit">OK</button>
    </form>

    <nav class="nav" aria-label="Navigation principale">
      <a href="/#home">Accueil</a>
      <a href="/#services">Services</a>
      <a href="/#gallery">Galerie</a>
      <a href="/contact" class="nav-link">Contact</a>
    </nav>
  </div>
</header>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    width: 100%;
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(16px);
    z-index: 1000;
    box-shadow: 0 16px 44px rgba(15, 23, 42, 0.08);
  }

  .navbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 18px 0;
  }

  .logo {
    display: flex;
    align-items: center;
    text-decoration: none;
    flex: 0 0 auto;
  }

  .search-form {
    flex: 1 1 420px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }

  .logo img {
    height: 64px;
    max-width: 220px;
    width: auto;
    object-fit: contain;
    transition: transform 0.25s ease, filter 0.25s ease;
  }

  .logo img:hover {
    transform: translateY(-2px);
    filter: drop-shadow(0 10px 16px rgba(18, 102, 241, 0.18));
  }

  .nav {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  .search-form input {
    width: 100%;
    max-width: 340px;
    padding: 0.65rem 0.9rem;
    border: 1px solid rgba(31, 41, 55, 0.12);
    border-radius: 999px;
    background: #f8fbff;
    color: #1f2937;
    outline: none;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }

  .search-form input:focus {
    border-color: rgba(18, 102, 241, 0.45);
    box-shadow: 0 0 0 4px rgba(18, 102, 241, 0.1);
  }

  .search-form button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.65rem 1rem;
    border-radius: 999px;
    border: none;
    background: #1266f1;
    color: #ffffff;
    font-weight: 600;
    cursor: pointer;
    transition: opacity 0.2s ease, transform 0.2s ease;
  }

  .search-form button:hover {
    opacity: 0.9;
    transform: translateY(-1px);
  }

  .nav a {
    text-decoration: none;
    color: #1f2937;
    font-weight: 600;
    position: relative;
    padding: 0.25rem 0;
  }

  .nav a::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: -6px;
    width: 0;
    height: 2px;
    background: #0d6efd;
    transition: width 0.3s ease;
  }

  .nav a:hover::after {
    width: 100%;
  }

  .nav-link {
    color: #1f2937;
  }

  @media (max-width: 960px) {
    .navbar {
      flex-direction: column;
      align-items: stretch;
      padding: 16px 0;
    }

    .nav {
      justify-content: center;
      flex-wrap: wrap;
      gap: 1rem;
      margin-top: 0.5rem;
    }

    .search-form {
      width: 100%;
      margin-top: 0.75rem;
      justify-content: center;
    }

    .search-form input {
      width: 100%;
      min-width: 0;
      max-width: none;
    }
  }

  @media (max-width: 640px) {
    .nav a {
      font-size: 0.95rem;
    }

    .logo img {
      height: 56px;
    }

    .search-form {
      flex-direction: column;
      gap: 0.75rem;
    }

    .search-form button {
      width: 100%;
    }
  }
</style>
<section id="home" class="hero">
  <div class="overlay"></div>

  <div class="container content reveal">
    <h1>Nettoyage professionnel pour entreprises et résidences</h1>

    <p>Des espaces impeccables, sains et parfaitement entretenus grâce à une équipe réactive et un service premium.</p>

    <div class="hero-actions">
      <a href="#contact" class="btn">Demander un devis</a>
      <a href="#services" class="btn secondary">Voir nos services</a>
    </div>

    <div class="hero-features">
      <span>✔ Intervention 24/7</span>
      <span>✔ Équipe certifiée</span>
      <span>✔ Produits écologiques</span>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    background-image: linear-gradient(180deg, rgba(18, 102, 241, 0.24), rgba(18, 102, 241, 0.05)), url('https://images.unsplash.com/photo-1581578731548-c64695cc6952?auto=format&fit=crop&w=1600&q=80');
    background-size: cover;
    background-position: center center;
    display: grid;
    align-items: center;
    position: relative;
    padding-top: 86px;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(4, 32, 80, 0.52), rgba(4, 32, 80, 0.12));
    z-index: 0;
  }

  .content {
    position: relative;
    z-index: 1;
    color: #f8fbff;
    text-align: center;
    padding: 0 1rem;
  }

  .content h1 {
    font-size: clamp(2.8rem, 5vw, 4.8rem);
    max-width: 820px;
    margin: 0 auto 1rem;
    line-height: 1.02;
    letter-spacing: -0.03em;
  }

  .content p {
    margin: 1.75rem auto 2rem;
    font-size: 1.15rem;
    max-width: 680px;
    color: rgba(255, 255, 255, 0.92);
  }

  .hero-actions {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 1rem;
    margin-top: 2rem;
  }

  .hero-features {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 0.85rem;
    margin-top: 1.8rem;
  }

  .hero-features span {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.8rem 1rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.14);
    color: #f8fbff;
    font-size: 0.95rem;
  }
</style>
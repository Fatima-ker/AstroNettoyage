<!-- CTA.astro -->

<section id="contact" class="cta reveal">

  <div class="container">

    <div class="cta-box">

      <span class="badge">
        ✨ Service Premium
      </span>

      <h2>
        Besoin d’un nettoyage professionnel ?
      </h2>

      <p>
        Contactez notre équipe dès aujourd’hui pour obtenir un devis gratuit et personnalisé.
      </p>

      <div class="cta-actions">

        <a href="/contact" class="btn white">
          Nous contacter
        </a>

        <a href="tel:+213555000000" class="btn outline">
          📞 Appeler
        </a>

      </div>

    </div>

  </div>

</section>

<style>
  .cta{
    padding:120px 0;
    background:
      linear-gradient(135deg, var(--secondary), var(--primary));
  }

  .cta-box{
    text-align:center;
    padding:70px 40px;
    border-radius:40px;
    background:rgba(255,255,255,0.1);
    backdrop-filter:blur(16px);
    color:white;
  }

  .badge{
    display:inline-block;
    padding:10px 18px;
    border-radius:999px;
    background:rgba(255,255,255,0.15);
    margin-bottom:24px;
  }

  .cta h2{
    font-size:clamp(2.5rem,5vw,4rem);
    margin-bottom:20px;
  }

  .cta p{
    max-width:700px;
    margin:auto;
    color:rgba(255,255,255,0.9);
  }

  .cta-actions{
    margin-top:2rem;
    display:flex;
    justify-content:center;
    gap:1rem;
    flex-wrap:wrap;
  }

  .white{
    background:white;
    color: var(--secondary);
  }

  .outline{
    border:2px solid white;
    background:transparent;
    color:white;
  }
</style>